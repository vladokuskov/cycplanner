import styled from 'styled-components';
import { ProfileSection } from './StyledProfileSection';

const ProfileSettingsSection = () => {
  return <ProfileSection title="Profile settings">1</ProfileSection>;
};

export { ProfileSettingsSection };
