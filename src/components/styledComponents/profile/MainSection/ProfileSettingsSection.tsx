import styled from 'styled-components';

const SettingsSectionWrapper = styled.section`
  width: 100%;
`;

const ProfileSettingsSection = () => {
  return <SettingsSectionWrapper>Settings section</SettingsSectionWrapper>;
};

export { ProfileSettingsSection };
