import styled from 'styled-components';

const StyledTitle = styled.h2`
  color: rgba(32, 32, 32, 0.77);
  font-style: normal;
  font-weight: 600;
  font-size: 2.9rem;
  line-height: 48px;
`;

export { StyledTitle };
